import type { FeaturedProjectsConfig } from '../models/configs';

export const featuredProjectsConfig: FeaturedProjectsConfig = {
    titleKey: 'app.features.featured-projects.title',
    descriptionKey: 'app.features.featured-projects.description',
    enableAutoScroll: true,
    showControls: true,
    delayBetweenSlides: 5000,
    showSlidebar: true,
};